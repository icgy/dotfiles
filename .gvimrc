set guifont=MigMix_2M:h10:w5 "font
colorscheme delek

set cursorline "Highlght current line
set guioptions-=T "Remove GUI
set guioptions-=m "Remove GUI
set cmdheight=1 "cmdline is only one
set winaltkeys=no "Don't focas menu to Alt+x

set clipboard=unnamed "Share with Window ClipBoard
au GUIEnter * simalt ~x "Fullscreen setup
